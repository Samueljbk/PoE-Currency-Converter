import requests
from .constants import POE_NINJA_URL, LEAGUE_NAME

def get_current_rates():
    # 1. Make API request to poe.ninja
    # 2. Process the JSON response
    # 3. Create and return dictionary of currency rates
    pass